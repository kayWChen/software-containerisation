spring:
  datasource:
    url: jdbc:mysql://localhost:3306/sc_score_system?useUnicode=true&characterEncoding=UTF-8
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true # print sql
    properties:
      hibernate:
        format_sql: true
server:
  port: 8181